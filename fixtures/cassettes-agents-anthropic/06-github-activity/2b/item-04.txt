VERDICT_CATEGORY: SUBTASK
MATCH_TASK_ID: t900
WORTH_A_CARD: real_task
GROUNDED: yes
ROUTING_OK: yes
RATIONALE: The retry backoff fix is part of the rate limiting work on the public API (t900, "one client's retry loop took the API down" ties directly to retry backoff logic), and is a small fix contained within that same effort rather than a standalone deliverable — it would close as part of that card's work, not tracked separately.